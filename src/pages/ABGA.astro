---
import Satz from "../components/Satz.astro";

const response = await fetch(
    "https://weichselriver.github.io/my_stamp_data/ABGA.json",
);
const data = await response.json();

console.log(data);
---

<h1>ABGA</h1>

{data.map((satz) => <Satz satz={satz} />)}
